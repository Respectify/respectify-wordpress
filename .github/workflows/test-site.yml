yname: Deploy to Test WordPress Site

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Change to repository directory
      run: cd ${{ github.workspace }} && cd respectify && echo "Respecitfy directory:" && pwd && ls -la

    - name: Deploy to SFTP server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SFTP_HOST }}
        username: ${{ secrets.SFTP_USERNAME }}
        password: ${{ secrets.SFTP_PASSWORD }}
        port: ${{ secrets.SFTP_PORT }}
        script: |
          echo "Current directory:"
          pwd
          echo "Contents of the current directory:"
          ls -la
          #mkdir -p /home/vintagedave/apps/respectify-test/wp-content/plugins/respectify
          #rsync -avz ./ /home/vintagedave/apps/respectify-test/wp-content/plugins/respectify
